name: zapis_zhivo
description: "Голосовой дневник"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^6.5.0
  path_provider: ^2.1.1
  permission_handler: ^11.3.0

flutter:
  uses-material-design: true
